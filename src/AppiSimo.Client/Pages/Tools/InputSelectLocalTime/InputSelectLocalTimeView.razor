@inherits InputSelectLocalTime

<MatSelect Class="@Class" @bind-Value="@Time" Enhanced="true">
            
    @foreach (var item in Items)
    {
        <MatOption Value="@($"{Converter.FormatValueAsString(item)}")">@Converter.FormatValueAsString(item)</MatOption>
    }
</MatSelect>
