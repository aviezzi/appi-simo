@inherits InputSelectLocalTime

<MatSelect Class="mt-3" @bind-Value="@Time" Enhanced="true">
            
    @foreach (var item in Items)
    {
        <MatOption Class="@Class" Value="@($"{Converter.FormatValueAsString(item)}")">@Converter.FormatValueAsString(item)</MatOption>
    }
</MatSelect>
